Fix Firebird dialect overrides that were shadowed after the feature-mixin
split: reorder the Firebird mixins ahead of their generic counterparts,
restore the dropped unsupported-feature formatters, and keep identifier
quoting consistent so generated DML matches DDL-created tables. Also compose
the core Sequence and Trigger mixins ahead of their protocol stubs so the
sequence/trigger DDL formatters and capability probes no longer resolve to
the no-op protocol stubs.
