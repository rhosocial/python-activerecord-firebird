Fixed Firebird mixin precedence so backend overrides, sequence/trigger DDL formatters, and capability probes resolve to their implementations rather than generic methods or protocol stubs; restored unsupported-feature formatters and consistent identifier quoting between DML and DDL; fail fast with UnsupportedFeatureError for WITH TIES, arrays, MATCH, WITHIN GROUP, and QUALIFY instead of emitting invalid SQL
