# types

Tests for Firebird 4.0+ data type mappings between Python and Firebird SQL types (`test_firebird_types.py`), and the FB4 version-gate rendering snapshots (TIMESTAMP/TIME WITH TIME ZONE, DECFLOAT, INT128 — `test_type_rendering.py`).
