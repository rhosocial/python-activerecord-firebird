# ddl

Firebird DDL: ALTER TABLE guards, identity columns, auto-increment, DROP TABLE cascade, and the cross-backend `CreateTableExpression.diff()` feature (`test_create_table_expression_diff.py`).

DOMAIN statement tests (Firebird-only) live in `../firebird/`.
