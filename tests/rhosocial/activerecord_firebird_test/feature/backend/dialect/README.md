# dialect

Firebird dialect tests: SQL-injection guarding (`test_dialect_security.py`) and dialect SQL branch snapshots (RETURNING / SKIP LOCKED / SEQUENCE / CREATE TABLE rendering, `test_dialect_branches.py`).

Schema-namespace declaration tests live in `../schema/`.
