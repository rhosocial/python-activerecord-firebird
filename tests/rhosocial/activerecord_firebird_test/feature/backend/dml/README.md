# dml

Firebird DML formatting: `ON CONFLICT` clause capability and the Firebird-specific `MERGE` statement renderer.
- `test_execute_many.py` — execute_many batch INSERT total-affected-row
  accounting and empty-parameter-list noop.
