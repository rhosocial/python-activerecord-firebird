# schema

Firebird schema-namespace tests: the `SchemaSupport` protocol declaration. Firebird has no schema namespaces — the database file is the entire namespace, so `supports_schema()` is explicitly False.
