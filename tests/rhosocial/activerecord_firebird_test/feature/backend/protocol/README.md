# protocol

Capability-protocol conformance for the Firebird backend.

Gap: `test_protocol_conformance.py` is not yet implemented here (Tier-2 Fill item, plan §6 matrix). The concurrency-protocol tests that previously lived in this directory moved to `../concurrency/`.
